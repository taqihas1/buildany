--- src/app/api/build/route.ts
+++ src/app/api/build/route.ts
@@ -148,7 +148,7 @@ async function buildProject(projectId: string, projectDir: string, outDir: strin
     if (isStaticHtml) {
       // Static HTML app: just copy files to out/
       console.log([Build] Detected static HTML app, copying files to out/...);
-      await copyDir(projectDir, outDir);
+      await copyDir(projectDir, outDir, ['out', '.git', 'node_modules']);
       // Remove .git from output
       try { await fs.rm(path.join(outDir, .git), { recursive: true, force: true }); } catch {}
       try { await fs.rm(path.join(outDir, node_modules), { recursive: true, force: true }); } catch {}
