# Memory Log: 2026-06-04

## BuildAny VPS Deployment (Hostinger)
- Hermes Agent: Running via Docker Manager, port 8642
- Traefik: Running for reverse proxy/routing
- OpenClaw: Being added via Docker Compose manually (YAML indentation issue fixed during this session)
- Docker Manager URL: https://hpanel.hostinger.com/vps/1730121/docker-manager
- Server IP: 89.39.210.236 (Hostinger VPS)

## API Keys Discussion
User asked about putting DeepSeek and Clerk API keys into secrets.
Provided 3 options:
1. GitHub Secrets (for CI/CD)
2. Docker Environment Variables (for VPS containers)
3. `.env.local` file on VPS (direct file)

**Status:** User has NOT yet chosen which option or provided keys. Waiting for their response.

## Docker Compose Fix
Fixed YAML indentation for adding OpenClaw to existing docker-compose.yaml. Correct format:
```yaml
version: '3.8'
services:
  openclaw:
    image: ghcr.io/openclaw/openclaw:latest
    container_name: openclaw
    restart: unless-stopped
    ports:
      - "3333:3333"
    volumes:
      - /root/openclaw-data:/data
    environment:
      - NODE_ENV=production
      - GATEWAY_PORT=3333
```

## Next Steps Pending
- User needs to provide project name for deployment
- User needs to choose where to store API keys
- After OpenClaw deploys, need to:
  - Install Node.js and PM2 on VPS
  - Clone buildany repo
  - Build and start BuildAny
  - Configure Nginx reverse proxy
  - Test Hermes API connectivity
  - Build BuildAny bridge to Hermes

## Technical Context
- Hermes API expected at: http://127.0.0.1:8642
- OpenClaw expected at: http://127.0.0.1:3333
- BuildAny will run on: http://127.0.0.1:3000
- Nginx will reverse proxy all three
- VPS specs: 2 vCPU / 8 GB RAM / 100 GB NVMe (Hostinger KVM 2)
