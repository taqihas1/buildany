# BuildAny Deployment Log - 2026-06-05

## Deployment Milestones

### 2026-06-05 ~10:00 UTC — SSL Certificate Acquired
- Let's Encrypt DNS-01 challenge completed for `base66.cloud` and `www.base66.cloud`
- Certificate path: `/etc/letsencrypt/live/base66.cloud/fullchain.pem`
- Key path: `/etc/letsencrypt/live/base66.cloud/privkey.pem`
- Expiry: 2026-09-03 (89 days)
- DNS TXT records used for validation (manual challenge)

### 2026-06-05 ~10:18 UTC — BuildAny App Deployed to VPS
- **VPS**: srv1730121, IP 2.25.170.135, Ubuntu Noble 24.04
- **App directory**: `/opt/buildany`
- **Node.js**: 22.22.3-1nodesource1 (via NodeSource)
- **Process manager**: PM2
- **App port**: 3000
- **App status**: `online` (PM2)
- **URL**: https://base66.cloud (live and serving HTML)

### Traefik Already Running on VPS
- **PID**: 193611
- **Ports**: 80 (HTTP), 443 (HTTPS)
- **Config**: `--providers.file.directory=/dynamic` (host mount: `/docker/traefik/dynamic`)
- **Dynamic config created**: `/docker/traefik/dynamic/buildany.yml`
- **Auto-SSL**: `certResolver=letsencrypt` via Traefik
- **HTTP→HTTPS redirect**: Configured
- **SSL cert**: Using Let's Encrypt certResolver
- **Proxy target**: `http://localhost:3000`
- Traefik uses Docker volume for `/letsencrypt` cert storage

### Key Traefik CLI Args (from ps aux):
```
traefik --api.dashboard=false --api.insecure=false --log.level=INFO \
  --providers.docker=true --providers.docker.exposedbydefault=false \
  --providers.file.directory=/dynamic --providers.file.watch=true \
  --entrypoints.web.address=:80 --entrypoints.websecure.address=:443 \
  --certificatesresolvers.letsencrypt.acme.httpchallenge=true \
  --certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web \
  --certificatesresolvers.letsencrypt.acme.email=admin@srv1730121.hstgr.cloud \
  --certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json \
  --entrypoints.web.http.redirections.entrypoint.to=websecure \
  --entrypoints.web.http.redirections.entrypoint.scheme=https
```

### Nginx NOT Used
- Nginx was installed but couldn't bind port 80/443 (Traefik already there)
- Nginx kept disabled: `systemctl stop nginx && systemctl disable nginx`
- Traefik handles all reverse proxy + SSL termination

### BuildAny App Status (Minimal Version)
- Next.js 15.0.0 with React 19.0.0
- Simple landing page: "BuildAny - AI App Builder - Deployed!"
- tsconfig.json created by Next.js auto-detection
- `moduleResolution: "bundler"` set manually to fix deprecated node10 warning
- Layout.tsx and globals.css created manually for build

## API Keys Provided by User
- **DeepSeek API Key**: `Sk-05d9bec6c9e8467b9ba95294add38a8e`
- **Clerk Publishable Key**: `pk_test_bm90ZWQtbmFyd2hhbC05OC5jbGVyay5hY2NvdW50cy5kZXYk`
- **Clerk Secret Key**: `sk_test_Vg2LoqrRabVR7r4vUHleWCyqpXFT7h7NI0NyM3dpqe`
- **Clerk Default Key**: `sk_test_Xiu5j3qmGGEElPLX0RlViIN1JEeQS52c03f1ooZcpN`
- **GitHub Token**: Not provided yet (2FA enabled on user account, but PATs bypass 2FA)
- **Expo Token**: Not provided yet (need for EAS Build)

### .env.local on VPS (`/opt/buildany/.env.local`)
```
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
DEEPSEEK_API_KEY=Sk-...
DATABASE_URL=./sqlite.db
```

## DNS Configuration
- **A Record**: `@` → `2.25.170.135` (TTL 3600)
- **CNAME**: `www` → `base66.cloud` (TTL 300)
- **TXT Records**: `_acme-challenge` and `_acme-challenge.www` used for cert validation
- **Provider**: Hostinger (DNS panel at hostinger.com)

## Docker on VPS
- Docker installed and running
- Existing containers: `bentopdf` (port 8080)
- Traefik running as process (not Docker container, but Docker volume for certs)
- Docker volume: `traefik_traefik-letsencrypt` mounted at `/letsencrypt`
- Docker compose files found: `/docker/traefik/docker-compose.yml`, `/docker/hermes-agent-cvaj/docker-compose.yml`

## Hermes Agent Present on VPS
- Docker compose found at `/docker/hermes-agent-cvaj/docker-compose.yml`
- This is relevant for future OpenClaw + Hermes integration discussions

## Pending Work (BuildAny Features)
1. Full BuildAny app with dashboard, prompt box, code generation, project workspace
2. Live preview component (iframe for web, phone frame for mobile)
3. Mobile export API (`/api/project/[id]/export/mobile`) with Expo SDK 54 templates
4. Web export API (`/api/project/[id]/export/web`) with Next.js output
5. GitHub export integration (needs GitHub PAT)
6. SQLite database with Drizzle ORM
7. LLM Router (DeepSeek, Kimi, GPT-4o) - API keys provided but not yet tested
8. Auto-research panel (`ResearchPanel.tsx` exists in codebase)
9. Agent Swarm dashboard (`SwarmDashboard.tsx` exists in codebase)
10. Clerk auth integration (keys provided, needs testing)

## Build Errors & Fixes Applied
- Missing `layout.tsx` → created with RootLayout component
- Missing `globals.css` → created with basic styles
- `moduleResolution=node10` deprecated → changed to `bundler` in tsconfig.json
- TypeScript dependencies missing → installed via npm
- `ERESOLVE` peer dependency conflicts with React 19 → used `--legacy-peer-deps`
- `npm run build` missing script → added to package.json

## Important Paths on VPS
- App: `/opt/buildany/`
- Traefik dynamic configs: `/docker/traefik/dynamic/`
- SSL certs: `/etc/letsencrypt/live/base66.cloud/`
- Traefik certs (Docker volume): `/var/lib/docker/volumes/traefik_traefik-letsencrypt/_data`
- Nginx config (disabled): `/etc/nginx/sites-available/buildany`
- PM2 process list: `/root/.pm2/dump.pm2`

## User Communication Notes
- User is on Kimi interface (kimi-claw channel)
- Extremely concise, imperative commands
- 
## BuildAny Mobile Pipeline — Started Development

- **2026-06-05 23:30+**: Began Phase 5 (Mobile Pipeline) per user request. VPS deployment deferred per user instruction.
- **Created files**:
  - `/root/buildany/src/app/api/project/[id]/generate/mobile/route.ts` — mobile generation API endpoint
  - `/root/buildany/src/lib/mobile-generator.ts` — full Expo SDK 54 template generator (package.json, app.json, tsconfig, eas.json, screen components, theme, button, header)
  - `/root/buildany/src/lib/ai-component-generator.ts` — stub for AI component generation
  - `/root/buildany/src/lib/ai-mobile-screen.ts` — stub for AI mobile screen generation
  - `/root/buildany/src/app/api/project/[id]/download/mobile/route.ts` — ZIP download for mobile exports
  - `/root/buildany/src/components/MobilePreview.tsx` — mobile preview UI with device frame, platform selector, generate/download buttons
- **Fixed imports**: Chat route was importing non-existent `generateCode`; updated to use `llmRouter` from `LLMRouter` class. The `LLMRouter` class has `generate()` and `stream()` methods, plus `parseGeneratedCode()` and `getSystemPromptForType()` helpers.
- **Schema note**: `projects.id` is `text` (string), not integer. Several routes had `parseInt(params.id)` which is wrong — needs `await params` then use `id` directly as string.
- **Build issues encountered**:
  - `JSZip` not installed (npm install failed due to lockfile conflicts)
  - Missing imports for `LivePreview` and `MobilePreview` in `ProjectWorkspace.tsx`
  - Type mismatches on `projectId` (number vs string) across routes
- **User decision**: "continue development task list and we defer install on vps and testing for later" — confirmed at 2026-06-05 21:27
- **GitHub repo**: `taqihas1/buildany` — main branch has code up to admin panel + API keys. VPS still has old placeholder due to git pull conflict (package-lock.json local changes).
