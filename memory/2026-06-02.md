
# TradePulse Development Session — June 2, 2026

## TaqList Watchlist + Buy/Sell Signals
- Added 38 pre-populated stocks from user's CSV to `src/data/taqList.ts`
- Added sector mapping (Energy, Tech, Semiconductors, etc.) to each stock
- Buy/Sell/Hold signals based on composite score (8+=Strong Buy, 6.5-8=Buy, 5-6.5=Hold, 3.5-5=Weak, <3.5=Sell)
- Color-coded score badges (green=strong, yellow=neutral, red=weak)
- Earnings alert banner when `daysUntilEarnings === 1`
- "Sector Pick" trophy badge for highest-scoring stock per sector
- All changes pushed to `taqihas1/tradepulse` via SSH

## Sector Recommendations Feature
- Horizontal scrollable sector filter chips on Watchlist screen
- SECTOR_ICONS and SECTOR_COLORS mapping for visual distinction
- Filter by sector (tap chip to filter, tap again to clear)
- Count badge per sector showing how many stocks in TaqList belong to that sector
- "All" chip shows total count

## EAS Build CI/CD Setup
- Created `eas.json` with preview/production profiles (iOS Ad Hoc, Android APK)
- Created `.github/workflows/eas-build.yml` with auto-retry (3x) on failure
- **User prefers MANUAL trigger** — not auto-on-push
- Workflow: `workflow_dispatch` only (no push trigger)
- iOS build: Ad Hoc distribution via EAS cloud (no local Mac needed)
- GitHub Actions just triggers EAS cloud build — actual compilation on EAS macOS servers

## User-Confirmed Patterns
- **Expo Go** for testing (`npx expo start --clear --offline`)
- **GitHub Actions + EAS** for real builds (manual trigger)
- Always uses SSH for GitHub push/fetch
- Never wants to test manually — delegates all testing to AI

## Skill Update
- Updated `expo-react-native-debug/SKILL.md` to include EAS Build CI/CD best practices
- Added Golden Rule #8: "Always use GitHub Actions for EAS Build — never build locally"
- Full section on EAS Build setup, GitHub Actions workflow, and iOS install options

## Next Steps for TradePulse (when user wants)
- Test current build via Expo Go
- Fix any bugs found
- Phase 2: Real stock data API (Alpha Vantage/Finnhub/Polygon)
- Phase 3: Sector breakdown recommendations (top picks per sector)
- Phase 4: Ads integration + polish

## New App Idea (Low Priority)
- **HotSell** — Amazon/Walmart trending items + Alibaba supplier matching
- Written to `memory/2026-06-01.md` for future reference
- Not started — TradePulse is current priority
