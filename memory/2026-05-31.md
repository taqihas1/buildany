# Memory — 2026-05-31

## RecipeWise App Fixes (Major Session)

### What We Fixed
- **Start Cooking button** — `app/[id].tsx` now scrolls to top + haptic feedback on press
- **TypeScript zero errors** — went from ~20 errors to clean compile
  - Added `card` color to `useColors()` hook + `lib/constants/theme.ts`
  - Added `containerClassName` prop to `ScreenContainer`
  - Created type declarations: `types/expo-symbols.d.ts`, `types/vitest.d.ts`, `types/expo-linear-gradient.d.ts`
  - Fixed `lib/image-resolver.ts` — `LOCAL_IMAGES` was undefined; replaced with `AI_IMAGES + UNSPLASH_IMAGES`
  - Renamed `lib/fridge.ts` → `lib/fridge-utils.ts` to avoid resolution conflict with `lib/fridge.tsx`
  - Re-exported `MealType` from `lib/store.ts`
  - Removed `"brunch"` from 3 recipes in `recipes-highprotein.ts` (type mismatch)
- **35 new recipes** (r216–r250) added to `lib/data/recipes-batch3.ts` — high protein, no pork, diverse cuisines
- **Recipe integration** — `BATCH3_RECIPES` merged into main `RECIPES` array so search/filter functions find new recipes
- **Quick Actions** — added Fridge, My Links, Favorites section to `app/index.tsx` home screen
- **Tab navigation** — promoted `fridge` and `my-links` from hidden routes to visible bottom tabs in `app/_layout.tsx`
- **Fridge screen** — created `app/fridge.tsx` as Expo Router wrapper for `lib/fridge.tsx`
- **AdMob config** — added `react-native-google-mobile-ads` plugin to `app.json` with test App IDs (needs real IDs before production)
- **Pork verification** — confirmed zero pork/ham/bacon/prosciutto/pepperoni recipes in all 250+ recipes

### Created Files
- `scripts/autoresearch-preflight.js` — quality gate script (tsc, tests, bundle, lint, pork check)
- `skills/autoresearch-swarm/SKILL.md` — AutoResearch ratchet loop + swarm deployment patterns
- `types/expo-symbols.d.ts`, `types/vitest.d.ts`, `types/expo-linear-gradient.d.ts`

### GitHub Push
- Pushed all commits to `taqihas1/RecipeWise` on `main` branch
- User confirmed clean pull to Mac laptop via SSH (`git pull origin main`)
- 4 commits total in this session

### Blocked/Learned
- Subagent spawning failed with `gateway closed (1008): pairing required` — loopback-only gateway `ws://127.0.0.1:18789` with token auth. Simulated swarm fallback (direct sequential work) works fine.
- User's test command: `npx tsc --noEmit` — only works in `recipewise-diagnostic` directory, not workspace root (there's no tsconfig in root)

## Next Open Tasks
- Replace AdMob test IDs with real `ca-app-pub-...` App IDs in `app.json`
- Test locally: `cd ~/recipewise && npx expo start --clear --offline`
- Build for production via EAS / GitHub Actions
- User has NOT tested the "Start Cooking" scroll fix on device yet — waiting for local test feedback
