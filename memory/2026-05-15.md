# 2026-05-15 - Recipe App Build Success!

## What happened today

After MANY failed EAS builds, we finally got a working APK!

### Build failures we fought through
1. **@trpc/react-query missing** - Removed tRPC packages from package.json, created stub files for lib/trpc.ts and lib/routers.ts
2. **react-native-worklets/plugin missing** - Was actually in package-lock.json, but npm ci on EAS failed because package.json had tRPC packages not in lock file
3. **npm ci lock file sync errors** - Fixed by removing tRPC packages (3 packages) so lock file matched package.json
4. **Expo doctor warnings** - Added .expo/ to .gitignore

### The winning build
- **Build ID**: 7bee22c4-46e6-427d-a63e-1119d5c7bd9e
- **Status**: FINISHED ✅
- **Duration**: ~16.5 minutes
- **APK URL**: https://expo.dev/artifacts/eas/giAKabuoKFxPzKbbcd9Bpu.apk
- **Local file**: /root/.openclaw/workspace/recipe-app.apk (76 MB)

### Key fixes that made it work
1. Removed `@trpc/react-query`, `@trpc/server`, `@tanstack/react-query` from package.json
2. Simplified `lib/trpc.ts` to a stub without tRPC imports
3. Simplified `lib/routers.ts` to a stub
4. Added `.expo/` to `.gitignore`
5. `react-native-worklets: 0.5.1` was already in lock file from earlier install

### Also updated
- `babel.config.js` - Added `plugins: ['react-native-worklets/plugin']` for next build
- Created stub `_core/` files (trpc.ts, systemRouter.ts, llm.ts, cookies.ts)
- Created `shared/const.js` stub

## Notes
- The import-recipe feature won't work without a backend server (tRPC), but the rest of the app works
- The app has 90 + 25 = 115 recipes total
- All 25 new recipes are high-protein, no pork/ham


---

## Build Confirmed Working (11:00 AM update)

After even MORE queued messages with the same old errors, user confirmed the build that finished was the SUCCESSFUL one. The APK is real and downloadable.

### EAS Build History (today's saga)
Multiple builds attempted before success:
- b6b88c09 (errored - tRPC missing)
- bf5798d4 (errored - tRPC missing)
- b8bc54c9 (errored - worklets plugin)
- 367fdd65 (errored - worklets plugin)
- 4dc26813 (errored - worklets plugin)
- 31a4873e (errored - various)
- And more canceled/errored builds...

**Winning build**: `7bee22c4-46e6-427d-a63e-1119d5c7bd9e` — **FINISHED** ✅

### Disk Space Crisis
Server was at 99-100% disk usage throughout. Multiple processes got SIGKILL'd:
- `expo export --platform web`
- `eas build` CLI
- `expo start`
- Metro bundler

Managed to free some space but it remained critically tight.

### Key Lesson
When `npm ci` fails on EAS with lockfile errors, check if package.json has packages that aren't in package-lock.json. `npm install` locally may succeed but `npm ci` on EAS will fail because it strictly validates the lockfile.
