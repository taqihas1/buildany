## 2026-05-20 - Server Issues & Dev Client Debugging

### Morning Session (9:00-9:54 AM)

**User issues:**
1. APK built via GitHub Actions crashes on Motorola 2021 phone (opens then closes)
2. Dev client installed on phone can't connect to server tunnel
3. "No address associated with hostname" error when connecting to exp.direct tunnel

**Investigated and tried:**
- Fixed `newArchEnabled=true` in GitHub workflow → set to `false`
- `npm install --legacy-peer-deps` completed successfully
- Pushed workflow changes to GitHub
- Killed multiple zombie Node processes blocking port 8081
- Started `npx expo start --tunnel` multiple times
- Tunnel URL confirmed working from server: `https://giyms48-aitrader-8081.exp.direct`
- Server responds with status 400 (which is correct for non-websocket requests)
- Metro bundler shows no errors in logs

**Root causes identified:**
1. **APK crash**: `newArchEnabled=true` in `gradle.properties` → fixed in GitHub workflow
2. **Standalone APK**: User says "tested many times, did not work"
3. **Dev client DNS issue**: Phone can't resolve `exp.direct` domains
4. **Server port conflicts**: Multiple zombie Node processes blocking port 8081

**Suggested fixes for DNS issue:**
- Change phone DNS to 8.8.8.8 / 1.1.1.1
- Switch between WiFi and mobile data
- Try direct IP `http://47.236.228.58:8081`
- Use a VPN app

**Status at end of session:**
- Server running on port 8081 with tunnel `https://giyms48-aitrader-8081.exp.direct`
- Awaiting user to try DNS fix or report back on which option worked
