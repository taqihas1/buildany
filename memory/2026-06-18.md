# Memory - 2026-06-18

## Kelly Tool-Calling Progress - CRITICAL

### What Was Fixed Today

1. **Kelly tool-calling endpoint WORKING** ✅
   - Kelly correctly calls `buildany_create_project` tool via `/api/hermes-chat`
   - Test command: `curl -s -X POST http://localhost:3000/api/hermes-chat -H "Content-Type: application/json" -d '{"message":"Create a new project called TestApp","projectId":"e20859e1-eebb-4bcf-8096-3e2704f1ff79"}'`
   - Kelly now responds with tool calls instead of hallucinating text responses

2. **"Unauthorized" error in tool execution** - CAUSED BY server-side HTTP fetch to `/api/projects`
   - When Kelly calls `buildany_create_project`, the handler did `fetch("http://localhost:3000/api/projects")`
   - This route requires Clerk auth → returns 401 "Unauthorized" → tool fails
   - Fix: Replace HTTP fetch with direct DB insert using Drizzle ORM

3. **Schema mismatch discovered** in `projects` table
   - Fields: `id`, `userId` (notNull), `name`, `description`, `type` (NOT `platform`), `status`, `githubRepo`, `createdAt`, `updatedAt`
   - `createdAt`/`updatedAt` auto-populate via `$defaultFn(() => new Date())` — don't need to set them
   - `userId` is required — used `"system"` as placeholder for Kelly-created projects
   - `type` field, not `platform` — must pass `"web" | "mobile" | "both"`

4. **Build errors encountered during fix**
   - `uuid` package not installed → replaced with `crypto.randomUUID()` (Node.js built-in)
   - TypeScript type errors from schema mismatch → fixed by using correct field names (`type` not `platform`, `userId` required)
   - Current status: Build in progress (TypeScript error at `route.ts:30` about insert values)

### Key Files Modified
- `src/app/api/hermes-chat/route.ts` — added DB imports, replaced `createProject` HTTP fetch with direct Drizzle insert

### Pending
- Fix TypeScript error at `route.ts:30` (insert values not matching schema)
- Rebuild and restart BuildAny
- Test Kelly end-to-end via browser at `https://base66.cloud/project/e20859e1-eebb-4bcf-8096-3e2704f1ff79`
- Wire Kelly into the UI "Build" button (currently shows old orchestrator messages like "Decomposed into 8 tasks")
- UI may be using `/api/orchestrate` instead of `/api/hermes-chat` — need to verify which flow the Build button triggers

### Architecture Understanding
- Kelly (Hermes gateway at localhost:8642) is the AI brain
- BuildAny (localhost:3000) is the body that executes tools
- `BUILDANY_TOOLS` array is baked into `route.ts` and sent to DeepSeek via Hermes
- When Kelly returns tool_calls, the route handler executes them directly (now via DB insert, not HTTP API)
- UI should use `useHermesChat` hook → calls `/api/hermes-chat` → forwards to Kelly → returns `{reply, toolCalls, toolResult, projectId}`
- Currently the UI may be using the old orchestrator flow (`/api/orchestrate`) which shows messages like "Decomposed into 8 tasks", "Wiki generation failed"

### Technical Notes
- Hermes container: `localhost:8642`, version `0.16.0`
- `stream: false` is required — SSE streaming breaks JSON parsing
- `tool_choice: "auto"` (not `"required"`) — works with DeepSeek via Hermes
- Kelly's system prompt: "You MUST use the available tools... You CANNOT write files or access filesystem directly"
- BuildAny PM2 process: `buildany`, runs on port 3000
- Next.js 16.2.7 with Turbopack
- SQLite DB at `/root/buildany/sqlite.db`

## Additional Notes - 2026-06-18 Session

### Build Error Fix In Progress
- **TypeScript error at `route.ts:40`**: `Type error: 'err' is of type 'unknown'`
  - Need to type-narrow the catch block: `if (err instanceof Error) { ... }`
  - This is a standard TypeScript strict-mode issue when catching errors

### Pattern for Future Fixes
When patching `route.ts` on VPS, always:
1. Check actual schema first with `grep -n -A 25 "export const projects = sqliteTable" src/lib/db/schema.ts`
2. Verify all required `.notNull()` fields are included in insert
3. Use `crypto.randomUUID()` instead of `uuid` package (not installed)
4. Type-narrow catch blocks: `const error = err instanceof Error ? err.message : 'Unknown error'`
5. After any patch, run `npm run build` before `pm2 restart` to catch TS errors early

### UI Integration Reminder
- The UI "Build" button may still be calling `/api/orchestrate` (old flow)
- Need to verify which API the UI actually calls when user clicks "Build"
- If using old orchestrator: shows messages like "Decomposed into 8 tasks", "Wiki generation failed"
- If using new Hermes flow: shows Kelly's actual response with tool calls
- Check `src/components/AIChatPanel.tsx` or `ChatOrchestratorPanel.tsx` to see which endpoint is used

### VPS Patching Technique (Working)
Using `node -e "..."` one-liners with `fs.readFileSync`/`replace`/`writeFileSync` is the fastest way to patch files when SSH is the only access method. Avoids heredoc escaping issues and works reliably.

### Next Session Priorities
1. Fix the `unknown` type error in catch block
2. Complete build and restart
3. Verify Kelly creates real projects via tool call
4. Check if UI is wired to `/api/hermes-chat` or old `/api/orchestrate`
5. If UI uses old flow, update it to use `useHermesChat` hook
