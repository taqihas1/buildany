# Recipe App Build Saga - May 15-16, 2026

## Current Status
EAS cloud builds failing during JS bundle or dependency install phase. Local builds impossible due to critically low disk space.

## Server Constraints
- Disk: 40GB total, ~37-38GB used, only ~0-400MB free
- Processes (npm install, Metro, Gradle, EAS CLI) getting SIGKILL'd by OOM/disk pressure
- Cannot run local Android builds or web export

## EAS Build Attempts Log
1. `31a4873e` - ~37s, UNKNOWN_ERROR at "Install dependencies"
2. `b8bc54c9` - ~40s, UNKNOWN_ERROR at "Install dependencies"
3. `bf5798d4` - ~50s, UNKNOWN_ERROR at "Install dependencies"
4. `367fdd65` - ~53s, UNKNOWN_ERROR at "Install dependencies"
5. `4dc26813` - ~53s, UNKNOWN_ERROR at "Bundle JavaScript" (with react-native-worklets@0.5.1)
6. `d307f7c9` - ~53s, UNKNOWN_ERROR at "Bundle JavaScript" (latest attempt)

## Key Technical Findings
- `.npmrc` with `legacy-peer-deps=true` is present but may not be respected by EAS `npm ci --include=dev`
- `react-native-worklets@0.5.1` installed as direct dependency (peer of react-native-reanimated)
- `react-native-worklets/plugin` needs to be LAST in babel.config.js plugins array
- tRPC packages (`@trpc/react-query`, `@trpc/server`, `@tanstack/react-query`) removed due to ERESOLVE but still imported by app code
- EAS build:view --json returns signed GCS URLs for log files (workaround for no dashboard access)
- Build logs show "Cannot find module 'react-native-worklets/plugin'" and tRPC import errors

## Expo SDK 54 Migration
- Upgraded from SDK 51 to 54
- react 19.1.0, react-native 0.81.5
- Many packages updated to SDK 54 versions
- Still 23 out-of-date packages per expo doctor

## Project Details
- Project: @aitrader/recipewise
- ID: 39382a67-e499-4031-bb4e-6cae2b3fcd18
- EAS account: taqihas@gmail.com / aitrader

## User Context
- User has dealership app and recipe app
- Wants APK/AAB deliverables
- Uses ngrok tunnel for testing
- Expects immediate, working solutions

## Next Steps (from last session)
1. Fetch latest build logs via GCS signed URL
2. Fix root cause (likely babel plugin order + missing tRPC deps)
3. Re-queue EAS build
4. If SDK 54 still fails, consider downgrade to SDK 52 (known stable)
5. Source zip fallback always available

## Useful Commands
- `eas build:view <id> --json | jq '.logFiles'` - get log URL
- `curl -sL "<logUrl>" | grep -i "error\|fail"` - quick error scan
- EAS_SKIP_AUTO_FINGERPRINT=1 eas build -p android --profile preview

## Failed Fix Attempts
- Stripping express/nativewind/tailwindcss → still failed, had to restore
- Adding react-native-worklets as direct dep → fixed local Metro but EAS still fails
- Downgrading react-native-worklets 0.8.3 → 0.5.1 → still fails in EAS

## Open Issues
- expo doctor: duplicate native modules (@expo/vector-icons, expo-constants, expo-font)
- .gitignore missing .expo/ and /android
- 23 out-of-date packages
- tRPC packages need re-installation with legacy-peer-deps
